<dl>
  <dt>Name</dt>
  <dd><%= @person.name %></dd>
  <dt>Birthday</dt>
  <dd><%= @person.birthday %></dd>
  <dt>Day of death</dt>
  <dd><%= @person.day_of_death %></dd>
  <dt>Note</dt>
  <dd><%= @person.note %></dd>
</dl>

<%= link_to 'edit', edit_person_path %>
<%= link_to 'index', people_path %>
<%= link_to 'destroy', @person, data: { confirm: 'Are you sure?' }, method: :delete %>
