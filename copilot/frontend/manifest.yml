name: frontend
type: Load Balanced Web Service

http:
  path: '/'
  healthcheck: '/'
  alias: 'frontend.shgtkshruch.com'

image:
  build: frontend/Dockerfile
  port: 80

cpu: 256
memory: 512
count: 1
exec: true
